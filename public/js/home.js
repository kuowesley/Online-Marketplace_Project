(function ($) {
  // let historyItems = $('#history-items')
  // let requestConfig = { method: "GET", url: "users/login/"};
  // $.ajax({
  //     method: "GET", url: "users/login/",
  //     success: function(data) {
  //         for (let i of data.message) {
  //             historyItems.append(`<li>${i}</li>`)
  //         }
  //     },
  //     error: function(xhr, status, error) {
  //     if (xhr.responseJSON && xhr.responseJSON.message) {
  //         alert(xhr.responseJSON.message)}
  //     else { alert(status) }
  //     }
  // })
})(window.jQuery);
